{
  "name": "genchat",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @genchat/shared && npm run build --workspace @genchat/extension && npm run build --workspace @genchat/api",
    "dev:extension": "npm run dev --workspace @genchat/extension",
    "dev:api": "npm run dev --workspace @genchat/api",
    "deploy:api": "npm run deploy --workspace @genchat/api",
    "release:tester": "node scripts/prepare-tester-release.mjs",
    "test": "npm run test --workspace @genchat/shared && npm run test --workspace @genchat/extension && npm run test --workspace @genchat/api"
  }
}
